{%- comment -%}
  ========================================
  Our Work Gallery — Forever Fierce
  Layout A (Editorial Bento) + Mobile M
  ========================================
{%- endcomment -%}

{%- liquid
  assign has_heroes = false
  for block in section.blocks
    if block.type == 'hero_shot' and block.settings.image != blank
      assign has_heroes = true
      break
    endif
  endfor
  assign has_archive = false
  for block in section.blocks
    if block.type == 'product_mockup' or block.type == 'lifestyle_shot'
      if block.settings.image != blank
        assign has_archive = true
        break
      endif
    endif
  endfor
-%}

<section class="ff-ow" data-section-id="{{ section.id }}">

  {%- if section.settings.eyebrow != blank or section.settings.title != blank or section.settings.subheading != blank -%}
  <header class="ff-ow__header">
    {%- if section.settings.eyebrow != blank -%}
      <p class="ff-ow__eyebrow">{{ section.settings.eyebrow | escape }}</p>
    {%- endif -%}
    {%- if section.settings.title != blank -%}
      <h1 class="ff-ow__title">{{ section.settings.title | escape }}</h1>
    {%- endif -%}
    {%- if section.settings.subheading != blank -%}
      <p class="ff-ow__sub">{{ section.settings.subheading | escape }}</p>
    {%- endif -%}
  </header>
  {%- endif -%}

  {%- comment -%} ---- Optional stats (desktop + mobile) ---- {%- endcomment -%}
  {%- for block in section.blocks -%}
    {%- if block.type == 'stats_strip' -%}
      <div class="ff-ow__stats" {{ block.shopify_attributes }}>
        {%- if block.settings.stat_1_number != blank -%}
          <div class="ff-ow__stat">
            <span class="ff-ow__stat-num">{{ block.settings.stat_1_number | escape }}</span>
            <span class="ff-ow__stat-label">{{ block.settings.stat_1_label | escape }}</span>
          </div>
        {%- endif -%}
        {%- if block.settings.stat_2_number != blank -%}
          <div class="ff-ow__stat">
            <span class="ff-ow__stat-num">{{ block.settings.stat_2_number | escape }}</span>
            <span class="ff-ow__stat-label">{{ block.settings.stat_2_label | escape }}</span>
          </div>
        {%- endif -%}
        {%- if block.settings.stat_3_number != blank -%}
          <div class="ff-ow__stat">
            <span class="ff-ow__stat-num">{{ block.settings.stat_3_number | escape }}</span>
            <span class="ff-ow__stat-label">{{ block.settings.stat_3_label | escape }}</span>
          </div>
        {%- endif -%}
        {%- if block.settings.stat_4_number != blank -%}
          <div class="ff-ow__stat">
            <span class="ff-ow__stat-num">{{ block.settings.stat_4_number | escape }}</span>
            <span class="ff-ow__stat-label">{{ block.settings.stat_4_label | escape }}</span>
          </div>
        {%- endif -%}
      </div>
    {%- endif -%}
  {%- endfor -%}

  {%- if has_heroes -%}

  {%- comment -%} ========== DESKTOP: Layout A Editorial Bento ========== {%- endcomment -%}
  <div class="ff-ow__desk" aria-hidden="false">

    {%- comment -%} Band 1 — open (1 feature + 2 stack) {%- endcomment -%}
    <div class="ff-ow__band ff-ow__band--open">
      {%- for block in section.blocks -%}
        {%- if block.type == 'hero_shot' and block.settings.role == 'open_feature' and block.settings.image != blank -%}
          {%- render 'our-work-tile', block: block, class: 'ff-ow__tile ff-ow__tile--feat', sizes: '(max-width: 900px) 100vw, 58vw', loading: 'eager' -%}
        {%- endif -%}
      {%- endfor -%}
      {%- for block in section.blocks -%}
        {%- if block.type == 'hero_shot' and block.settings.role == 'open_stack' and block.settings.image != blank -%}
          {%- render 'our-work-tile', block: block, class: 'ff-ow__tile', sizes: '(max-width: 900px) 100vw, 38vw', loading: 'eager' -%}
        {%- endif -%}
      {%- endfor -%}
    </div>

    {%- comment -%} Band 2 — portrait triptych {%- endcomment -%}
    <div class="ff-ow__band ff-ow__band--3 ff-ow__band--portrait">
      {%- for block in section.blocks -%}
        {%- if block.type == 'hero_shot' and block.settings.role == 'triptych' and block.settings.image != blank -%}
          {%- render 'our-work-tile', block: block, class: 'ff-ow__tile', sizes: '(max-width: 900px) 50vw, 33vw' -%}
        {%- endif -%}
      {%- endfor -%}
    </div>

    {%- comment -%} Band 3 — detail / landscape strip {%- endcomment -%}
    <div class="ff-ow__band ff-ow__band--3 ff-ow__band--land">
      {%- for block in section.blocks -%}
        {%- if block.type == 'hero_shot' and block.settings.role == 'detail' and block.settings.image != blank -%}
          {%- render 'our-work-tile', block: block, class: 'ff-ow__tile', sizes: '(max-width: 900px) 50vw, 33vw' -%}
        {%- endif -%}
      {%- endfor -%}
    </div>

    {%- comment -%} Band 4 — color pair {%- endcomment -%}
    <div class="ff-ow__band ff-ow__band--color">
      {%- for block in section.blocks -%}
        {%- if block.type == 'hero_shot' and block.settings.role == 'color' and block.settings.image != blank -%}
          {%- render 'our-work-tile', block: block, class: 'ff-ow__tile', sizes: '(max-width: 900px) 100vw, 50vw' -%}
        {%- endif -%}
      {%- endfor -%}
    </div>

    {%- comment -%} Band 5 — bento {%- endcomment -%}
    <div class="ff-ow__band ff-ow__band--bento">
      {%- for block in section.blocks -%}
        {%- if block.type == 'hero_shot' and block.settings.role == 'bento_feature' and block.settings.image != blank -%}
          {%- render 'our-work-tile', block: block, class: 'ff-ow__tile ff-ow__tile--feat', sizes: '(max-width: 900px) 100vw, 55vw' -%}
        {%- endif -%}
      {%- endfor -%}
      {%- for block in section.blocks -%}
        {%- if block.type == 'hero_shot' and block.settings.role == 'bento_stack' and block.settings.image != blank -%}
          {%- render 'our-work-tile', block: block, class: 'ff-ow__tile', sizes: '(max-width: 900px) 100vw, 40vw' -%}
        {%- endif -%}
      {%- endfor -%}
    </div>

    {%- comment -%} Band 6 — close craft {%- endcomment -%}
    <div class="ff-ow__band ff-ow__band--close">
      {%- for block in section.blocks -%}
        {%- if block.type == 'hero_shot' and block.settings.role == 'close' and block.settings.image != blank -%}
          {%- render 'our-work-tile', block: block, class: 'ff-ow__tile', sizes: '(max-width: 900px) 100vw, 720px' -%}
        {%- endif -%}
      {%- endfor -%}
    </div>

  </div>

  {%- comment -%} ========== MOBILE: Layout M Product Scroll ========== {%- endcomment -%}
  <div class="ff-ow__mobi" aria-hidden="true">

    {%- comment -%} 1 Full-bleed hero {%- endcomment -%}
    <div class="ff-ow-m__hero">
      {%- for block in section.blocks -%}
        {%- if block.type == 'hero_shot' and block.settings.role == 'open_feature' and block.settings.image != blank -%}
          {%- render 'our-work-tile', block: block, class: 'ff-ow__tile', sizes: '100vw', loading: 'eager', show_cap: true -%}
        {%- endif -%}
      {%- endfor -%}
    </div>

    {%- comment -%} 2 First open_stack as inset quiet {%- endcomment -%}
    {%- assign inset_done = false -%}
    {%- for block in section.blocks -%}
      {%- if block.type == 'hero_shot' and block.settings.role == 'open_stack' and block.settings.image != blank and inset_done == false -%}
        <div class="ff-ow-m__inset">
          {%- render 'our-work-tile', block: block, class: 'ff-ow__tile', sizes: '92vw', show_cap: true -%}
        </div>
        {%- assign inset_done = true -%}
      {%- endif -%}
    {%- endfor -%}

    {%- comment -%} 3 First two triptych as 2-up pair {%- endcomment -%}
    <div class="ff-ow-m__pair">
      {%- assign pair_count = 0 -%}
      {%- for block in section.blocks -%}
        {%- if block.type == 'hero_shot' and block.settings.role == 'triptych' and block.settings.image != blank and pair_count < 2 -%}
          {%- render 'our-work-tile', block: block, class: 'ff-ow__tile', sizes: '48vw', show_cap: true -%}
          {%- assign pair_count = pair_count | plus: 1 -%}
        {%- endif -%}
      {%- endfor -%}
    </div>

    {%- comment -%} 4 Horizontal detail rail {%- endcomment -%}
    <p class="ff-ow-m__rail-label">Print detail</p>
    <div class="ff-ow-m__rail" aria-label="Horizontal detail gallery">
      {%- for block in section.blocks -%}
        {%- if block.type == 'hero_shot' and block.settings.role == 'detail' and block.settings.image != blank -%}
          {%- render 'our-work-tile', block: block, class: 'ff-ow__tile', sizes: '78vw', show_cap: true -%}
        {%- endif -%}
      {%- endfor -%}
      {%- for block in section.blocks -%}
        {%- if block.type == 'hero_shot' and block.settings.role == 'close' and block.settings.image != blank -%}
          {%- render 'our-work-tile', block: block, class: 'ff-ow__tile', sizes: '78vw', show_cap: true -%}
        {%- endif -%}
      {%- endfor -%}
    </div>

    {%- comment -%} 5 First color as full-bleed break {%- endcomment -%}
    {%- assign color_i = 0 -%}
    {%- for block in section.blocks -%}
      {%- if block.type == 'hero_shot' and block.settings.role == 'color' and block.settings.image != blank -%}
        {%- if color_i == 0 -%}
          <div class="ff-ow-m__bleed">
            {%- render 'our-work-tile', block: block, class: 'ff-ow__tile', sizes: '100vw', show_cap: true -%}
          </div>
        {%- endif -%}
        {%- assign color_i = color_i | plus: 1 -%}
      {%- endif -%}
    {%- endfor -%}

    {%- comment -%} 6 Alternating full / inset stack {%- endcomment -%}
    <div class="ff-ow-m__stack">
      {%- comment -%} remaining color, remaining triptych, bento, remaining open_stack {%- endcomment -%}
      {%- assign color_i = 0 -%}
      {%- for block in section.blocks -%}
        {%- if block.type == 'hero_shot' and block.settings.role == 'color' and block.settings.image != blank -%}
          {%- if color_i > 0 -%}
            {%- render 'our-work-tile', block: block, class: 'ff-ow__tile', sizes: '100vw', show_cap: true -%}
          {%- endif -%}
          {%- assign color_i = color_i | plus: 1 -%}
        {%- endif -%}
      {%- endfor -%}

      {%- for block in section.blocks -%}
        {%- if block.type == 'hero_shot' and block.settings.role == 'bento_feature' and block.settings.image != blank -%}
          <div class="ff-ow-m__inset">
            {%- render 'our-work-tile', block: block, class: 'ff-ow__tile', sizes: '92vw', show_cap: true -%}
          </div>
        {%- endif -%}
      {%- endfor -%}

      {%- assign trip_i = 0 -%}
      {%- for block in section.blocks -%}
        {%- if block.type == 'hero_shot' and block.settings.role == 'triptych' and block.settings.image != blank -%}
          {%- if trip_i >= 2 -%}
            {%- render 'our-work-tile', block: block, class: 'ff-ow__tile', sizes: '100vw', show_cap: true -%}
          {%- endif -%}
          {%- assign trip_i = trip_i | plus: 1 -%}
        {%- endif -%}
      {%- endfor -%}

      {%- assign bento_i = 0 -%}
      {%- for block in section.blocks -%}
        {%- if block.type == 'hero_shot' and block.settings.role == 'bento_stack' and block.settings.image != blank -%}
          {%- if bento_i == 0 -%}
            <div class="ff-ow-m__inset">
              {%- render 'our-work-tile', block: block, class: 'ff-ow__tile', sizes: '92vw', show_cap: true -%}
            </div>
          {%- else -%}
            {%- render 'our-work-tile', block: block, class: 'ff-ow__tile', sizes: '100vw', show_cap: true -%}
          {%- endif -%}
          {%- assign bento_i = bento_i | plus: 1 -%}
        {%- endif -%}
      {%- endfor -%}

      {%- assign open_i = 0 -%}
      {%- for block in section.blocks -%}
        {%- if block.type == 'hero_shot' and block.settings.role == 'open_stack' and block.settings.image != blank -%}
          {%- if open_i > 0 -%}
            <div class="ff-ow-m__inset">
              {%- render 'our-work-tile', block: block, class: 'ff-ow__tile', sizes: '92vw', show_cap: true -%}
            </div>
          {%- endif -%}
          {%- assign open_i = open_i | plus: 1 -%}
        {%- endif -%}
      {%- endfor -%}
    </div>

    {%- comment -%} 7 Close craft {%- endcomment -%}
    <div class="ff-ow-m__close">
      {%- for block in section.blocks -%}
        {%- if block.type == 'hero_shot' and block.settings.role == 'close' and block.settings.image != blank -%}
          {%- render 'our-work-tile', block: block, class: 'ff-ow__tile', sizes: '92vw', show_cap: true -%}
        {%- endif -%}
      {%- endfor -%}
    </div>

  </div>

  {%- endif -%}

  {%- comment -%} ---- Optional archive grid (legacy mockups) ---- {%- endcomment -%}
  {%- if has_archive -%}
    {%- if section.settings.archive_eyebrow != blank -%}
      <p class="ff-ow__archive-eyebrow">{{ section.settings.archive_eyebrow | escape }}</p>
    {%- endif -%}
    <div class="ff-ow__archive">
      {%- for block in section.blocks -%}
        {%- case block.type -%}
          {%- when 'product_mockup' -%}
            {%- if block.settings.image != blank -%}
              {%- render 'our-work-tile', block: block, class: 'ff-ow__tile ff-ow__tile--square', sizes: '(max-width: 768px) 50vw, 33vw' -%}
            {%- endif -%}
          {%- when 'lifestyle_shot' -%}
            {%- if block.settings.image != blank -%}
              {%- render 'our-work-tile', block: block, class: 'ff-ow__tile ff-ow__tile--wide', sizes: '(max-width: 768px) 100vw, 66vw' -%}
            {%- endif -%}
        {%- endcase -%}
      {%- endfor -%}
    </div>
  {%- endif -%}

  {%- if section.settings.closing_text != blank -%}
    <p class="ff-ow__closing">{{ section.settings.closing_text | escape }}</p>
  {%- endif -%}

  {%- if section.settings.cta_text != blank and section.settings.cta_link != blank -%}
    <div class="ff-ow__foot">
      <a href="{{ section.settings.cta_link }}" class="ff-ow__cta">{{ section.settings.cta_text | escape }}</a>
    </div>
  {%- endif -%}

  <div class="ff-ow-lb" id="ff-ow-lb-{{ section.id }}" role="dialog" aria-modal="true" aria-label="Image viewer" aria-hidden="true">
    <button type="button" class="ff-ow-lb__close" aria-label="Close image viewer">&times;</button>
    <button type="button" class="ff-ow-lb__nav ff-ow-lb__nav--prev" aria-label="Previous image">
      <svg viewBox="0 0 24 24" width="28" height="28" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"></polyline></svg>
    </button>
    <div class="ff-ow-lb__stage">
      <img class="ff-ow-lb__img" alt="" width="1600" height="1600">
      <div class="ff-ow-lb__meta">
        <p class="ff-ow-lb__caption"></p>
        <p class="ff-ow-lb__counter"></p>
      </div>
    </div>
    <button type="button" class="ff-ow-lb__nav ff-ow-lb__nav--next" aria-label="Next image">
      <svg viewBox="0 0 24 24" width="28" height="28" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"></polyline></svg>
    </button>
  </div>

</section>

<style>
  .ff-ow {
    --ff-ow-ink: #0B0B0B;
    --ff-ow-bone: #F2EDE4;
    --ff-ow-soft: #2a2a2a;
    --ff-ow-muted: #6b6b6b;
    --ff-ow-gap: 10px;
    --ff-ow-r: 4px;
    max-width: 1440px;
    margin: 0 auto;
    padding: 72px 20px 96px;
    font-family: inherit;
    color: var(--ff-ow-ink);
    background: transparent;
  }
  .ff-ow__header { text-align: center; margin-bottom: 40px; max-width: 720px; margin-left: auto; margin-right: auto; }
  .ff-ow__eyebrow {
    font-size: 11px; letter-spacing: 0.16em; text-transform: uppercase;
    color: var(--ff-ow-muted); margin: 0 0 14px; font-weight: 600;
  }
  .ff-ow__title {
    font-size: clamp(40px, 7vw, 72px); font-weight: 800; margin: 0 0 18px;
    letter-spacing: -0.03em; line-height: 0.95; text-transform: uppercase; color: var(--ff-ow-ink);
  }
  .ff-ow__sub {
    font-size: 15px; color: var(--ff-ow-soft); max-width: 560px; margin: 0 auto; line-height: 1.65;
  }

  /* stats */
  .ff-ow__stats {
    background: var(--ff-ow-ink); border-radius: var(--ff-ow-r); padding: 36px 24px;
    display: grid; grid-template-columns: repeat(4, 1fr); gap: 16px; margin-bottom: 28px;
  }
  .ff-ow__stat { text-align: center; color: #fff; }
  .ff-ow__stat-num { font-size: 28px; font-weight: 700; letter-spacing: -0.02em; display: block; line-height: 1; }
  .ff-ow__stat-label {
    font-size: 10px; letter-spacing: 0.14em; text-transform: uppercase;
    opacity: 0.65; display: block; margin-top: 10px; font-weight: 500;
  }

  /* tiles */
  .ff-ow__tile {
    position: relative; display: block; width: 100%; height: 100%;
    background: #111; border-radius: var(--ff-ow-r); overflow: hidden;
    padding: 0; border: 0; cursor: zoom-in; font: inherit; color: inherit;
  }
  .ff-ow__tile img {
    width: 100%; height: 100%; object-fit: cover; display: block;
    transition: transform 0.7s cubic-bezier(0.2, 0.7, 0.2, 1);
  }
  button.ff-ow__tile:hover img,
  button.ff-ow__tile:focus-visible img { transform: scale(1.04); }
  button.ff-ow__tile:focus-visible { outline: 2px solid #93D500; outline-offset: 3px; }
  .ff-ow__cap {
    position: absolute; left: 0; right: 0; bottom: 0;
    padding: 28px 14px 12px;
    background: linear-gradient(transparent, rgba(0,0,0,0.55));
    color: #fff; font-size: 10px; font-weight: 600; letter-spacing: 0.1em;
    text-transform: uppercase; opacity: 0; transform: translateY(6px); transition: 0.3s ease;
    text-align: left;
  }
  button.ff-ow__tile:hover .ff-ow__cap,
  button.ff-ow__tile:focus-visible .ff-ow__cap { opacity: 1; transform: none; }
  .ff-ow__cap--always { opacity: 1; transform: none; }

  /* ===== Desktop Layout A ===== */
  .ff-ow__desk { display: block; }
  .ff-ow__mobi { display: none; }
  .ff-ow__band { display: grid; gap: var(--ff-ow-gap); margin-bottom: var(--ff-ow-gap); }
  .ff-ow__band--open {
    grid-template-columns: 1.35fr 0.85fr;
    grid-template-rows: 1fr 1fr;
    min-height: min(72vh, 780px);
  }
  .ff-ow__band--open .ff-ow__tile--feat { grid-row: 1 / -1; }
  .ff-ow__band--3 { grid-template-columns: repeat(3, 1fr); }
  .ff-ow__band--portrait .ff-ow__tile { aspect-ratio: 3 / 4; height: auto; }
  .ff-ow__band--land .ff-ow__tile { aspect-ratio: 5 / 4; height: auto; }
  .ff-ow__band--color {
    grid-template-columns: 1.1fr 0.9fr;
    min-height: min(56vh, 600px);
  }
  .ff-ow__band--bento {
    grid-template-columns: 1.2fr 0.9fr;
    grid-template-rows: 1fr 1fr;
    min-height: min(64vh, 700px);
  }
  .ff-ow__band--bento .ff-ow__tile--feat { grid-row: 1 / -1; }
  .ff-ow__band--close { grid-template-columns: 1fr; }
  .ff-ow__band--close .ff-ow__tile {
    max-width: 920px; margin: 0 auto; aspect-ratio: 5 / 4; height: auto; width: 100%;
  }

  /* archive */
  .ff-ow__archive-eyebrow {
    font-size: 11px; letter-spacing: 0.16em; text-transform: uppercase;
    color: var(--ff-ow-muted); font-weight: 600; text-align: center;
    margin: 48px 0 16px;
  }
  .ff-ow__archive {
    display: grid; grid-template-columns: repeat(3, 1fr); gap: var(--ff-ow-gap);
    margin-bottom: 40px;
  }
  .ff-ow__tile--square { aspect-ratio: 1 / 1; height: auto; }
  .ff-ow__tile--wide { grid-column: span 2; aspect-ratio: 2 / 1; height: auto; }

  .ff-ow__closing {
    text-align: center; font-size: 15px; color: var(--ff-ow-soft);
    max-width: 520px; margin: 40px auto 24px; line-height: 1.65;
  }
  .ff-ow__foot { text-align: center; }
  .ff-ow__cta {
    display: inline-block; font-size: 12px; letter-spacing: 0.1em; text-transform: uppercase;
    padding: 16px 28px; background: var(--ff-ow-ink); color: #fff; text-decoration: none;
    font-weight: 700; border-radius: 2px; transition: background 0.2s, color 0.2s;
  }
  .ff-ow__cta:hover, .ff-ow__cta:focus { background: #93D500; color: var(--ff-ow-ink); }

  /* lightbox */
  .ff-ow-lb {
    position: fixed; inset: 0; background: rgba(10,10,10,0.96); z-index: 9999;
    display: none; align-items: center; justify-content: center; padding: 40px 60px;
    opacity: 0; transition: opacity 0.2s ease;
  }
  .ff-ow-lb.is-open { display: flex; opacity: 1; }
  .ff-ow-lb__stage { max-width: 100%; max-height: 100%; display: flex; flex-direction: column; align-items: center; gap: 16px; }
  .ff-ow-lb__img {
    max-width: 100%; max-height: calc(100vh - 160px); width: auto; height: auto;
    object-fit: contain; display: block; border-radius: 4px;
  }
  .ff-ow-lb__meta { text-align: center; color: rgba(255,255,255,0.85); min-height: 20px; }
  .ff-ow-lb__caption { font-size: 14px; margin: 0 0 6px; letter-spacing: 0.02em; }
  .ff-ow-lb__caption:empty { display: none; }
  .ff-ow-lb__counter { font-size: 11px; letter-spacing: 0.15em; text-transform: uppercase; opacity: 0.5; margin: 0; }
  .ff-ow-lb__close {
    position: absolute; top: 20px; right: 24px; width: 44px; height: 44px; border-radius: 50%;
    background: rgba(255,255,255,0.08); border: 0; color: #fff; font-size: 28px; line-height: 1;
    cursor: pointer; display: flex; align-items: center; justify-content: center; padding: 0;
  }
  .ff-ow-lb__close:hover, .ff-ow-lb__close:focus-visible { background: rgba(255,255,255,0.18); outline: none; }
  .ff-ow-lb__nav {
    position: absolute; top: 50%; transform: translateY(-50%); width: 52px; height: 52px;
    border-radius: 50%; background: rgba(255,255,255,0.08); border: 0; color: #fff;
    cursor: pointer; display: flex; align-items: center; justify-content: center; padding: 0;
  }
  .ff-ow-lb__nav:hover, .ff-ow-lb__nav:focus-visible { background: rgba(255,255,255,0.18); outline: none; }
  .ff-ow-lb__nav--prev { left: 24px; }
  .ff-ow-lb__nav--next { right: 24px; }
  body.ff-ow-lb-open { overflow: hidden; }

  /* ===== Mobile Layout M ===== */
  @media (max-width: 900px) {
    .ff-ow { padding: 48px 0 64px; }
    .ff-ow__header { padding: 0 20px; margin-bottom: 28px; }
    .ff-ow__title { font-size: clamp(36px, 11vw, 48px); }
    .ff-ow__sub { font-size: 14px; }
    .ff-ow__stats {
      margin: 0 16px 20px; grid-template-columns: repeat(2, 1fr); gap: 20px; padding: 28px 16px;
    }
    .ff-ow__desk { display: none; }
    .ff-ow__mobi { display: block; }

    .ff-ow-m__hero .ff-ow__tile { border-radius: 0; aspect-ratio: 3 / 4; height: auto; }
    .ff-ow-m__inset { padding: 0 18px; margin: 14px 0; }
    .ff-ow-m__inset .ff-ow__tile { border-radius: 6px; aspect-ratio: 4 / 5; height: auto; }
    .ff-ow-m__pair {
      display: grid; grid-template-columns: 1fr 1fr; gap: 6px;
      padding: 0 6px; margin-bottom: 14px;
    }
    .ff-ow-m__pair .ff-ow__tile { aspect-ratio: 3 / 4; height: auto; border-radius: 4px; }
    .ff-ow-m__pair .ff-ow__cap { font-size: 8px; padding: 20px 8px 8px; }

    .ff-ow-m__rail-label {
      font-size: 10px; font-weight: 700; letter-spacing: 0.14em; text-transform: uppercase;
      color: var(--ff-ow-muted); padding: 10px 18px 8px; margin: 0;
    }
    .ff-ow-m__rail {
      display: flex; gap: 8px; overflow-x: auto; scroll-snap-type: x mandatory;
      -webkit-overflow-scrolling: touch; padding: 0 18px 16px; margin-bottom: 8px;
      scrollbar-width: none;
    }
    .ff-ow-m__rail::-webkit-scrollbar { height: 0; }
    .ff-ow-m__rail .ff-ow__tile {
      flex: 0 0 78%; aspect-ratio: 5 / 4; height: auto;
      border-radius: 6px; scroll-snap-align: center;
    }

    .ff-ow-m__bleed .ff-ow__tile { border-radius: 0; aspect-ratio: 1 / 1; height: auto; }
    .ff-ow-m__stack .ff-ow__tile { border-radius: 0; aspect-ratio: 4 / 5; height: auto; margin-bottom: 8px; }
    .ff-ow-m__stack .ff-ow-m__inset { margin-bottom: 14px; }
    .ff-ow-m__stack .ff-ow-m__inset .ff-ow__tile { margin-bottom: 0; }
    .ff-ow-m__close { padding: 8px 18px 0; }
    .ff-ow-m__close .ff-ow__tile { border-radius: 6px; aspect-ratio: 5 / 4; height: auto; }

    /* captions always on for touch */
    .ff-ow__mobi .ff-ow__cap { opacity: 1; transform: none; }
    .ff-ow__mobi button.ff-ow__tile:hover img { transform: none; }

    .ff-ow__archive { grid-template-columns: 1fr 1fr; padding: 0 12px; gap: 8px; }
    .ff-ow__tile--wide { grid-column: 1 / -1; }
    .ff-ow__archive-eyebrow { padding: 0 20px; }
    .ff-ow__closing { padding: 0 20px; margin-top: 32px; font-size: 14px; }
    .ff-ow__foot { padding: 0 20px; }
    .ff-ow__cta { width: 100%; max-width: 360px; text-align: center; }

    .ff-ow-lb { padding: 60px 12px 80px; }
    .ff-ow-lb__img { max-height: calc(100vh - 200px); }
    .ff-ow-lb__nav { width: 44px; height: 44px; }
    .ff-ow-lb__nav--prev { left: 8px; }
    .ff-ow-lb__nav--next { right: 8px; }
  }

  @media (prefers-reduced-motion: reduce) {
    .ff-ow__tile img, .ff-ow-lb { transition: none; }
    button.ff-ow__tile:hover img { transform: none; }
  }
</style>

<script>
(function() {
  var section = document.querySelector('[data-section-id="{{ section.id }}"]');
  if (!section) return;
  var triggers = section.querySelectorAll('[data-lightbox-trigger]');
  if (!triggers.length) return;
  var lb = section.querySelector('.ff-ow-lb');
  var lbImg = lb.querySelector('.ff-ow-lb__img');
  var lbCaption = lb.querySelector('.ff-ow-lb__caption');
  var lbCounter = lb.querySelector('.ff-ow-lb__counter');
  var btnClose = lb.querySelector('.ff-ow-lb__close');
  var btnPrev = lb.querySelector('.ff-ow-lb__nav--prev');
  var btnNext = lb.querySelector('.ff-ow-lb__nav--next');

  // Dedupe desk/mobi clones by shot id
  var items = [];
  var indexById = {};
  Array.prototype.forEach.call(triggers, function(t) {
    var id = t.getAttribute('data-shot-id') || t.getAttribute('data-full');
    if (indexById[id] != null) return;
    indexById[id] = items.length;
    items.push({
      id: id,
      full: t.getAttribute('data-full'),
      srcset: t.getAttribute('data-full-srcset'),
      caption: t.getAttribute('data-caption') || '',
      alt: t.querySelector('img') ? t.querySelector('img').alt : ''
    });
  });

  var currentIndex = 0;
  var lastFocused = null;

  function show(index) {
    currentIndex = (index + items.length) % items.length;
    var item = items[currentIndex];
    lbImg.src = item.full;
    if (item.srcset) lbImg.setAttribute('srcset', item.srcset);
    else lbImg.removeAttribute('srcset');
    lbImg.alt = item.alt;
    lbCaption.textContent = item.caption;
    lbCounter.textContent = (currentIndex + 1) + ' / ' + items.length;
  }
  function openById(id) {
    var idx = indexById[id];
    if (idx == null) idx = 0;
    lastFocused = document.activeElement;
    show(idx);
    lb.classList.add('is-open');
    lb.setAttribute('aria-hidden', 'false');
    document.body.classList.add('ff-ow-lb-open');
    btnClose.focus();
  }
  function close() {
    lb.classList.remove('is-open');
    lb.setAttribute('aria-hidden', 'true');
    document.body.classList.remove('ff-ow-lb-open');
    lbImg.removeAttribute('src');
    lbImg.removeAttribute('srcset');
    if (lastFocused && lastFocused.focus) lastFocused.focus();
  }

  Array.prototype.forEach.call(triggers, function(t) {
    t.addEventListener('click', function() {
      var id = t.getAttribute('data-shot-id') || t.getAttribute('data-full');
      openById(id);
    });
  });
  btnClose.addEventListener('click', close);
  btnPrev.addEventListener('click', function() { show(currentIndex - 1); });
  btnNext.addEventListener('click', function() { show(currentIndex + 1); });
  lb.addEventListener('click', function(e) { if (e.target === lb) close(); });
  document.addEventListener('keydown', function(e) {
    if (!lb.classList.contains('is-open')) return;
    if (e.key === 'Escape') { e.preventDefault(); close(); }
    else if (e.key === 'ArrowLeft') { e.preventDefault(); show(currentIndex - 1); }
    else if (e.key === 'ArrowRight') { e.preventDefault(); show(currentIndex + 1); }
  });
  var touchStartX = null;
  lb.addEventListener('touchstart', function(e) {
    if (e.touches.length === 1) touchStartX = e.touches[0].clientX;
  }, { passive: true });
  lb.addEventListener('touchend', function(e) {
    if (touchStartX === null) return;
    var dx = e.changedTouches[0].clientX - touchStartX;
    if (Math.abs(dx) > 50) {
      if (dx > 0) show(currentIndex - 1);
      else show(currentIndex + 1);
    }
    touchStartX = null;
  });
})();
</script>

{% schema %}
{
  "name": "Our Work Gallery",
  "tag": "section",
  "class": "section-our-work",
  "settings": [
    { "type": "header", "content": "Page header" },
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "Custom gym apparel" },
    { "type": "text", "id": "title", "label": "Title", "default": "Our Work" },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Subheading",
      "default": "Real shirts from real gym drops — close enough to judge the print yourself."
    },
    { "type": "header", "content": "Archive (optional)" },
    {
      "type": "text",
      "id": "archive_eyebrow",
      "label": "Archive section label",
      "default": "More from the archive",
      "info": "Shown only if Product mockup or Lifestyle blocks exist."
    },
    { "type": "header", "content": "Closing" },
    {
      "type": "textarea",
      "id": "closing_text",
      "label": "Closing paragraph",
      "default": "Every drop is built around your gym. Free design. Members preorder. You don’t buy inventory."
    },
    { "type": "text", "id": "cta_text", "label": "Button text", "default": "Schedule a call" },
    { "type": "url", "id": "cta_link", "label": "Button link" }
  ],
  "blocks": [
    {
      "type": "hero_shot",
      "name": "Hero shot",
      "settings": [
        {
          "type": "select",
          "id": "role",
          "label": "Layout role",
          "info": "Controls desktop band + mobile placement. Use the install map for the 15-shot set.",
          "default": "triptych",
          "options": [
            { "value": "open_feature", "label": "Open — large feature (1)" },
            { "value": "open_stack", "label": "Open — stack (2)" },
            { "value": "triptych", "label": "Portrait triptych (3)" },
            { "value": "detail", "label": "Detail strip / mobile rail (3–4)" },
            { "value": "color", "label": "Color pair (2)" },
            { "value": "bento_feature", "label": "Bento — large feature (1)" },
            { "value": "bento_stack", "label": "Bento — stack (2)" },
            { "value": "close", "label": "Close craft (1)" }
          ]
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image",
          "info": "Upload ~1600–2200px long edge, WebP/JPEG ~150–400KB. Do not force square."
        },
        { "type": "text", "id": "alt", "label": "Alt text" },
        { "type": "text", "id": "caption", "label": "Caption (optional)" }
      ]
    },
    {
      "type": "product_mockup",
      "name": "Archive mockup (square)",
      "settings": [
        { "type": "image_picker", "id": "image", "label": "Square mockup" },
        { "type": "text", "id": "alt", "label": "Alt text" },
        { "type": "text", "id": "caption", "label": "Lightbox caption (optional)" }
      ]
    },
    {
      "type": "lifestyle_shot",
      "name": "Archive lifestyle (wide)",
      "settings": [
        { "type": "image_picker", "id": "image", "label": "Wide photo" },
        { "type": "text", "id": "alt", "label": "Alt text" },
        { "type": "text", "id": "caption", "label": "Lightbox caption (optional)" }
      ]
    },
    {
      "type": "stats_strip",
      "name": "Stats strip",
      "limit": 1,
      "settings": [
        { "type": "text", "id": "stat_1_number", "label": "Stat 1 number", "default": "30,000+" },
        { "type": "text", "id": "stat_1_label", "label": "Stat 1 label", "default": "Orders" },
        { "type": "text", "id": "stat_2_number", "label": "Stat 2 number", "default": "1.8M" },
        { "type": "text", "id": "stat_2_label", "label": "Stat 2 label", "default": "Shirts printed" },
        { "type": "text", "id": "stat_3_number", "label": "Stat 3 number", "default": "5,000+" },
        { "type": "text", "id": "stat_3_label", "label": "Stat 3 label", "default": "Gyms" },
        { "type": "text", "id": "stat_4_number", "label": "Stat 4 number", "default": "50" },
        { "type": "text", "id": "stat_4_label", "label": "Stat 4 label", "default": "States" }
      ]
    }
  ],
  "max_blocks": 60,
  "presets": [
    {
      "name": "Our Work Gallery",
      "blocks": [
        { "type": "hero_shot", "settings": { "role": "open_feature" } },
        { "type": "hero_shot", "settings": { "role": "open_stack" } },
        { "type": "hero_shot", "settings": { "role": "open_stack" } },
        { "type": "hero_shot", "settings": { "role": "triptych" } },
        { "type": "hero_shot", "settings": { "role": "triptych" } },
        { "type": "hero_shot", "settings": { "role": "triptych" } },
        { "type": "hero_shot", "settings": { "role": "detail" } },
        { "type": "hero_shot", "settings": { "role": "detail" } },
        { "type": "hero_shot", "settings": { "role": "detail" } },
        { "type": "hero_shot", "settings": { "role": "color" } },
        { "type": "hero_shot", "settings": { "role": "color" } },
        { "type": "hero_shot", "settings": { "role": "bento_feature" } },
        { "type": "hero_shot", "settings": { "role": "bento_stack" } },
        { "type": "hero_shot", "settings": { "role": "bento_stack" } },
        { "type": "hero_shot", "settings": { "role": "close" } }
      ]
    }
  ]
}
{% endschema %}
